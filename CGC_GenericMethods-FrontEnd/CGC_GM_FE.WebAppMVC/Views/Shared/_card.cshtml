@model CGC_GM_FE.WebAppMVC.ViewModels.CommonViewModel

<div class="col-sm-12 col-lg-3">
    <div class="card text-white bg-primary mb-0">
        <div class="card-header">
            @Model.Card_Image()
        </div>
        <div class="card-body">
            @Model.Card_TitlePage()
            @Model.Card_SubTitlePage()
        </div>
    </div>
</div>
