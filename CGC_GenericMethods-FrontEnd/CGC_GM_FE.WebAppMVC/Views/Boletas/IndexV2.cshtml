@model IEnumerable<CGC_GM_FE.Common.Models.Boleta>

@{
    ViewBag.Title = "Boletas";
}

@section Breadcrumb{
    <li class="breadcrumb-item">@Html.ActionLink("Dashboard", "Index", "Dashboard")</li>
    <li class="breadcrumb-item active">Boletas</li>
}

<p>
    @Html.ActionLink("Crear", "Create", null, new { @class = "btn btn-success btn-sm" })
</p>
<table class="table table-sm table-bordered table-hover table-striped">
    <tr class="text-center">
        <th>
            @Html.DisplayNameFor(model => model.NumeroBoleta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TiempoEfectivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TiempoInvertidoEn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaRegistro)
        </th>
        <th>Acciones</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroBoleta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiempoEfectivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiempoInvertidoEn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td class="text-center">
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "btn btn-warning btn-sm" })&nbsp;
                @Html.ActionLink("Detalles", "Details", new { id = item.Id }, new { @class = "btn btn-info btn-sm" })&nbsp;
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id }, new { @class = "btn btn-danger btn-sm btnDelete" })
            </td>
        </tr>
    }
</table>